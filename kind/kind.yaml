# This config file contains all config fields with comments
# NOTE: this is not a particularly useful config file
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
# 1 control plane node and 3 workers
nodes:
# the control plane node config
- role: control-plane
# the three workers
- role: worker
- role: worker
- role: worker

# Cluster with ingress support
# kind: Cluster
# apiVersion: kind.x-k8s.io/v1alpha4
# nodes:
# - role: control-plane
#   kubeadmConfigPatches:
#   - |
#     kind: InitConfiguration
#     nodeRegistration:
#       kubeletExtraArgs:
#         node-labels: "ingress-ready=true"
#   extraPortMappings:
#   - containerPort: 80
#     hostPort: 80
#     protocol: TCP
#   - containerPort: 443
#     hostPort: 443
#     protocol: TCP
# # the three workers
# - role: worker
# - role: worker
# - role: worker


# kind cluster with networking config and custom subnet values for pod and service subnets
# kind: Cluster
# apiVersion: kind.x-k8s.io/v1alpha4
# networking:
#     podSubnet: "10.214.0.0/16"
#     serviceSubnet: "10.16.0.0/12"
# nodes:
# - role: control-plane
# - role: worker
# - role: worker


# https://medium.com/@muthanagavamsi/install-kubernetes-on-windows-11-with-kind-8b06c303d09c
